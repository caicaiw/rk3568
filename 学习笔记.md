# RKNN 通用 C API 学习笔记

> 记录：从 CMake 构建 → main.cc 推理 → 板端运行的完整流程，以及踩过的坑。

---

## 一、项目整体构建流程

```
┌─ 前置条件
│   ├─ 交叉编译器：/usr/local/arm64/gcc-arm-10.3-2021.07-x86_64-aarch64-none-linux-gnu/
│   └─ 第三方库：3rdparty/（librknn_api、opencv）
│
├─ 第 1、2 步：写 CMakeLists.txt + main.cc（先后顺序无所谓）
│   ├─ CMakeLists.txt：告诉 CMake "怎么构建"
│   │   ├─ set(...)          定义变量（路径、架构）
│   │   ├─ include_directories()  头文件搜索路径（编译用）
│   │   ├─ find_package(OpenCV)   找 OpenCV
│   │   ├─ add_executable(名字 源文件...)  生成可执行目标
│   │   ├─ target_link_libraries()  链接库
│   │   └─ install(...)      安装规则
│   └─ main.cc：调用 RKNN API 写推理逻辑
│
├─ 第 3 步：运行 build.sh → 内部三步
│   │   cmake（读 CMakeLists → 生成 Makefile）
│   │   make（gcc 编译 main.cc + 链接 → 生成 example）
│   │   make install（拷到 install/example_Linux/）
│   └─ 产出结构：
│       install/example_Linux/
│       ├── example            ← 可执行程序
│       ├── model/             ← 模型（RK3568/RK3588 的 .rknn）
│       └── lib/librknnrt.so   ← 运行时库
│
└─ 第 4 步：推送到板端，运行
    ├─ 把整个 install/example_Linux/ 拷到板子
    ├─ 运行时找动态库（librknnrt.so 在 lib/ 下）：
    │       export LD_LIBRARY_PATH=lib
    └─ 运行：
            ./example model/RK3588/resnet18.rknn 你的图片.jpg
```

---

## 二、知识版图

### 1. CMake 核心概念
| 概念 | 说明 |
|------|------|
| `set(变量 值)` | 给变量赋值，等价于 bash 的 `变量=值` |
| `${变量}` | 取变量的值 |
| `CMAKE_SOURCE_DIR` | 最顶层 CMakeLists.txt 所在目录（这个工程 = 00_example/） |
| `add_executable(名字 源文件...)` | 生成可执行目标（第一个参数是名字，之后是源文件列表） |
| `target_link_libraries` | 链接库（如 librknnrt.so、OpenCV） |
| `include_directories` | 头文件搜索路径（编译时用） |
| `install(... DESTINATION ...)` | 安装规则：装什么、装到哪 |
| `TARGETS / DIRECTORY / PROGRAMS` | 安装对象类型：CMake 编译的 / 文件夹 / 现成文件 |

### 2. 工具链层级（重要！）
```
CMakeLists.txt → cmake → 生成 Makefile → make → 调用 gcc/g++
   (你的配置)   (生成器)               (调度)   (真正编译)
```
- **gcc/g++**：真正的编译器（把 .cpp 变机器码）
- **make**：包工头（调度 gcc，不是编译器！）
- **cmake**：读 CMakeLists.txt，生成 Makefile

### 3. 交叉编译
- 在 x86_64 电脑上，编译出在 aarch64（ARM 板子）上运行的程序
- 工具链名字：`aarch64-none-linux-gnu-gcc` = 前缀 `aarch64-none-linux-gnu` + `-gcc`
- 架构对应：`x86_64` = 电脑，`aarch64` = 板子

### 4. Shell 脚本基础（build.sh）
| 语法 | 意思 |
|------|------|
| `#!/bin/bash` | shebang：宣告用 bash 执行（第一行特殊，其余 `#` 是注释） |
| `set -e` | 任何命令出错立刻停 |
| `变量=值` / `${变量}` | 赋值 / 取值 |
| `if [[ ! -d "路径" ]]; then ... fi` | 判断目录不存在才创建 |
| `# 注释` | 注释（bash 不执行） |

### 5. C/C++ 基础（复习重点）
- **指针**：存的是"地址"。`char *p = "hello"` 存首字符地址；`char *p2 = p` 复制地址，指向同一个目标
- **数组**：`int a[5]` 声明（5 个元素）；`a[2]` 访问（下标从 0）。**数组名在传参时自动变成指向首元素的指针**
- **结构体**：`typedef struct _xxx {...} xxx;` = 声明结构体 + 起别名（`xxx` 是公开名，`_xxx` 是内部名）
- **typedef**：给类型起别名，如 `typedef uint64_t rknn_context;`
- **声明 vs 定义**：头文件只有声明（无 `{}`）；实现在 `.so` 库或 `.cc` 里
- **编译 vs 链接**：编译看头文件（检查调用对不对）→ 生成 `.o`；链接把调用和 `.so` 里的实现连上 → 生成可执行文件
- **&（取地址）**：函数要"写"外部变量就得传地址；只读就传值
- **类型 ↔ 字节**：int8=1字节，int32/float=4字节，uint64=8字节；`size/4` = float 的个数
- **内存管理**："谁分配谁释放"——RKNN 分配的输出 buf 用 `rknn_outputs_release`，RKNN 创建的会话用 `rknn_destroy`；OpenCV 图片和栈上数组自动释放

### 6. RKNN API 调用流程（main.cc 主线）
```
1. rknn_context context;                声明会话句柄（本质 uint64 编号）
2. rknn_init(&context, model, 0, 0, NULL)  加载模型 + 初始化会话（size=0 表示 model 是路径）
3. rknn_input_output_num io_num;        定义"输入输出数量"结构体
4. rknn_query(context, RKNN_QUERY_IN_OUT_NUM, &io_num, sizeof(...))  查询数量
5. cv::imread / cv::cvtColor            读图 + BGR→RGB 预处理
6. rknn_input inputs[1];                定义输入信息数组（填 type/fmt/size/buf）
7. rknn_inputs_set(context, n_input, inputs)  把输入提交给模型
8. rknn_run(context, NULL)              真正推理（可用 gettimeofday 计时）
9. rknn_output outputs[1];              定义输出信息数组（is_prealloc=0 让 RKNN 分配）
10. rknn_outputs_get(context, n_output, outputs, NULL)  取结果
11. 后处理：size/4 = float 个数，rknn_GetTop 挑 Top5
12. rknn_outputs_release(...)           释放输出缓冲
13. rknn_destroy(context)               销毁会话
```

### 7. 零拷贝（zero copy）—— 与通用 API 对比
| | 通用 API | 零拷贝 |
|--|---------|--------|
| 数据怎么给 NPU | `inputs[0].buf`（CPU 内存），RKNN 内部**拷贝**到 NPU | `rknn_create_mem` 申请**共享内存**（CPU+NPU 都能访问），**无拷贝** |
| 访问成员 | 结构体变量 `.`（`inputs[0].buf`） | 结构体指针 `->`（`input_mem[0]->virt_addr`） |
| 关键 API | `rknn_inputs_set` | `rknn_create_mem` / `rknn_set_io_mem` |
| 释放 | `rknn_outputs_release` | `rknn_destroy_mem` |

**零拷贝完整流程：**
```
1. rknn_query(RKNN_QUERY_INPUT_ATTR / OUTPUT_ATTR)  查询输入输出张量属性（多大、什么格式）
2. rknn_create_mem(context, size_with_stride)        申请共享内存（CPU+NPU 都能访问）
3. memcpy(input_mem->virt_addr, img.data, ...)       CPU 把图片数据写进共享内存
4. rknn_set_io_mem(context, mem, attr)               把"内存"和"张量"绑定（告诉模型用哪块内存）
5. rknn_run(context, NULL)                           NPU 直接在共享内存上推理
```
- 输入：**CPU 写内存、NPU 读**；输出：**NPU 写内存、CPU 读**
- 省掉的是"CPU↔NPU 之间一次数据拷贝"（**不是**省预处理，预处理仍是 CPU 做）
- **对齐（alignment）**：数据地址要满足硬件"按块读内存"的边界要求；`size_with_stride` = 数据 + 对齐填充，内存要给 NPU 直读必须用它来申请

### 8. 模型转换链条（PC 端 rknn-toolkit2）
```
.pt（PyTorch 训练产物，PyTorch 专用，FP32）
  ↓ torch.onnx.export(模型, 示例输入, 'xxx.onnx')   导出
.onnx（通用中间格式，跨框架标准，FP32）
  ↓ rknn-toolkit2：config → load → build(量化) → export_rknn
.rknn（RKNPU 专用，INT8 量化，板端用 librknnrt.so 推理）
```
| 格式 | 谁生成 | 精度 | 在哪用 |
|------|--------|------|--------|
| `.pt` | PyTorch 训练 | FP32 | PyTorch 环境 |
| `.onnx` | `torch.onnx.export` | FP32 | 通用（任何框架） |
| `.rknn` | rknn-toolkit2 | INT8（量化） | RKNPU 板端 |

- ONNX = 通用"原料"，RKNN = 板端"成品"
- `load_pytorch` 可直接加载 `.pt`（不转 onnx 也行）；但**转 onnx 更保险**（通用、toolkit2 支持最成熟，个别算子直接读 PyTorch 可能踩坑）
- `build(do_quantization=True, dataset=...)` = 用 dataset 图片把 FP32 量化为 INT8（省内存、加速）
- `.npy` = NumPy 数组文件（`np.save` 存、`np.load` 读）；test.py 把模型 3 个输出存成 `.npy`，用于和 C 代码/板端结果对比验证

### 9. yolov5 目标检测结构
```
输入图片 → Backbone(提取特征) → Neck(多尺度融合) → Head(3个尺度输出预测) → 后处理(过滤+NMS)
```
- 目标检测 = **同时**定位（框 bbox）+ 分类（类别），**不是**"先分类再画框"
- `outputs[0/1/2]` = Head 在 **3 个尺度**上的原始输出（后处理前）
- 每个网格位置预测：框 `(x,y,w,h)` + 置信度 + 各类别概率
- 后处理：`filter_boxes`（阈值过滤低置信度框）+ `nms_boxes`（NMS 去重）→ 画出最终框

### 10. 其他易混概念
- **int8 的位数**：int8 = 8 个二进制位（无符号 0~255 / 有符号 -128~127），不是"三位十进制"
- **对齐 alignment**：数据地址要满足硬件按块读的边界要求；`size_with_stride` = 数据 + 对齐填充
- **attr** = attribute（属性）：描述对象的特征信息，不是对象本身
- **include 写法**：系统库用 `<>`（直接找系统目录），自己写的/工程内头文件用 `""`（先找当前目录再回落系统）
- **相对路径**：`'yolov5s.rknn'` = 相对当前运行目录（等价 `'./yolov5s.rknn'`）；`src/main.cc` 相对 CMakeLists 所在目录
- **SDK** = Software Development Kit（软件开发工具包：头文件 + 库 + 文档 + 示例）
- **删除目录**：`rmdir`（只能删空目录）、`rm -r`（递归删非空）、`rm -rf`（强制，**慎用**！）；`delete` 是 Windows 命令，Linux 没有
- **memset/memcpy** 的声明在 `<string.h>`（要 include；`<string>` 是 C++ string 类，不含这些声明）

---

## 三、遇到的问题与解决（踩坑记录）

### 问题 1：VS Code 找不到头文件（红波浪线）
- **现象**：`#include "rknn_api.h"` / opencv 报"无法打开源文件"
- **原因**：C/C++ 扩展（IntelliSense）不会自己读 CMakeLists.txt，需要单独告诉它头文件路径
- **解决**：创建 `.vscode/c_cpp_properties.json`，在 `includePath` 里写 `3rdparty/librknn_api/include` 和 `3rdparty/opencv/include`
- **记住**：编译靠 CMakeLists.txt（第 1 层）；编辑器提示靠 c_cpp_properties.json（第 2 层）。两者独立！

### 问题 2：stdint.h 找不到（系统头文件）
- **现象**：rknn_api.h 能打开了，但报 `cannot open source file "stdint.h" (dependency of "rknn_api.h")`
- **原因**：`stdint.h` 是系统标准头文件，扩展靠 `compilerPath` 里的编译器去"探测"系统头文件目录。而 `/usr/bin/gcc` 不存在（没装系统 gcc）
- **解决**：`sudo apt install gcc g++ -y`，然后 `Ctrl+Shift+P → Reload Window` 重新加载
- **记住**：编译器自己知道系统库在哪 → 只要 compilerPath 指向真实存在的编译器即可

### 问题 3：Windows 解压不了交叉编译器压缩包
- **现象**：`.tar.xz` 在 Windows 解压失败
- **原因**：① Windows 不认 `.tar.xz` 格式；② 包里有符号链接和可执行权限，Windows 文件系统不支持，解出来也是坏的
- **解决**：传到 Ubuntu 再解压：`sudo tar -xJf 包名 -C /usr/local/arm64`
- **记住**：工具链天生是给 Linux 用的，必须在 Linux 里解压

### 问题 4：cp 不能把文件从 Windows 拷到 Ubuntu
- **原因**：Windows 和虚拟机 Ubuntu 是两个独立文件系统，`cp` 只在本机内部工作
- **解决**：用 VMware 共享文件夹 / 拖拽 / scp（跨机器用 scp，本机内部才用 cp）
- **记住**："同一台电脑" ≠ "同一个文件系统"；cp=本机，scp=跨机

### 问题 5：`Exec format error` 把压缩包当程序执行
- **现象**：`bash xxx.tar.gz` 报 `cannot execute binary file: Exec format error`
- **原因**：把 `.tar.gz` 压缩包当成"可执行程序"去运行了
- **解决**：压缩包要**解压**（`tar -xzf`）不是执行
- **记住**：`.tar.gz` 用 `-xzf`，`.tar.xz` 用 `-xJf`

### 问题 6：`cmake: command not found`
- **原因**：系统没装 cmake（它是程序，要先安装）
- **解决**：`sudo apt install cmake make -y`，然后 `cmake --version` 验证
- **记住**：cmake / make / gcc 都是程序，缺哪个就 apt 装哪个

### 问题 7：`chmod -777 build.sh` 反而 Permission denied
- **现象**：`./build.sh` 报 Permission denied，`chmod -777 build.sh` 后更严重
- **原因**：`chmod -777` 的 `-` 是"移除"权限，不是"设置"！`-777` = 把读/写/执行全删掉（变成 000）
- **解决**：`chmod +x build.sh`（只加执行）或 `chmod 777 build.sh`（直接设）
- **记住**：`+` = 加权限，`-` = 减权限，`777` = rwxrwxrwx（所有人可读可写可执行）

### 问题 8：`RKNN_TENSOR_INT8` vs `UINT8` 导致输出错乱（零拷贝示例）
- **现象**：板端推理 Top5 结果全乱（top1 类别 562 之类，和图片毫无关系）
- **原因**：输入 type 设成 `RKNN_TENSOR_INT8`（有符号 -128~127），但图片像素是 **0~255 无符号**，128~255 被读成负数 → 输入数据错乱
- **解决**：改成 `RKNN_TENSOR_UINT8`（无符号，正好对应 0~255 像素）→ 输出正常（top1 = 812 = space shuttle，和图片吻合）
- **记住**：**像素 0~255 是无符号 → 输入用 UINT8**；输出概率是小数 → 用 FLOAT32。**输入图快（INT8/UINT8），输出图准（FLOAT32）**
- 附：`INT8` 读像素是"按补码解读"（128~255 变 -128~-1），**不是**"截断到 127"

### 问题 9：`rmdir` 目录非空 / `delete` 未找到命令
- **现象**：`rmdir xxx/` 报"目录非空"；敲 `delete xxx/` 报"未找到命令"
- **原因**：`rmdir` 只能删**空目录**；`delete` 是 Windows/DOS 的命令，Linux 没有
- **解决**：非空目录用 `rm -r 目录名`（递归删）；强制可加 `-f`（`rm -rf`，慎用）
- **记住**：Linux 删东西用 `rm`；`rmdir` 只能删空的

---

## 四、一句话口诀总结
- **编译看"怎么用"（头文件声明），链接找"在哪用"（.so 里的实现）**
- **谁分配，谁释放**
- **add_executable 告诉 CMake，CMake 写进 Makefile，make 照着调 gcc**
- **输入是你给的（自己准备 buf），输出是模型给的（让 RKNN 分配）**
